clone:
  depth: 1
kind: pipeline
name : Forza
steps:
 - name: SM6250
   image: nexgang/ubuntu:latest
   environment:
     token:
       from_secret: token
     chat_id:
       from_secret: chat_id
   commands:
      -  bash build.sh
